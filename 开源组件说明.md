# 新增工具的开源组件

2026年9月更新。界面、名单管理和业务逻辑由本项目实现，以下组件通过npm安装并随构建打包，不依赖外部CDN。

| 工具 | 使用的开源项目 | 用途 | 许可证 |
| --- | --- | --- | --- |
| 抽签大转盘 | [CrazyTim/spin-wheel](https://github.com/CrazyTim/spin-wheel) | 转盘绘制、动画和指针定位 | MIT |
| 名单导入 | [mholt/PapaParse](https://github.com/mholt/PapaParse) | CSV解析 | MIT |
| 名单导入 | [catamphetamine/read-excel-file](https://github.com/catamphetamine/read-excel-file) | Excel .xlsx读取 | MIT |
| 视频加水印 | [Vanilagy/mediabunny](https://github.com/Vanilagy/mediabunny) | 视频解码、逐帧叠加与音视频编码封装 | MPL-2.0 |
| 计算器 | [josdejong/mathjs](https://github.com/josdejong/mathjs) | 算式解析、高精度小数、科学运算 | Apache-2.0 |

短链接研究参考：[miantiao-me/Sink](https://github.com/miantiao-me/Sink)。本项目独立实现了简化的Cloudflare D1存储、短码生成、有效期与跳转逻辑，没有复制Sink源码。

各组件的完整许可证位于对应npm包中，锁定版本见package-lock.json。Mediabunny源码地址如上，本次没有修改其库文件。

## 短链接部署

数据库：`eason-toolbox-links`，绑定：`SHORT_LINKS`。

首次部署前执行 `npx wrangler d1 migrations apply eason-toolbox-links --remote`。
配置同时包含每分钟20次创建限制的 `LINK_LIMITER` 绑定。

`types/cloudflare-env.d.ts`由Wrangler生成，添加了`export type { Cloudflare }`使运行时类型局限在模块内，避免与浏览器DOM类型冲突。重新生成时保留这个模块导出。

新增页面：`/lottery-wheel`、`/video-watermark`、`/short-link`、`/calculator`。
短链接入口：`/s/短码`；创建接口：`POST /api/short-links`。
